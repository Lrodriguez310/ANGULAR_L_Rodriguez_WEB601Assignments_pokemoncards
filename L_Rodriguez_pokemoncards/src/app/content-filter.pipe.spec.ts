import { ContentFilterPipe } from './content-filter.pipe';

describe('ContentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
